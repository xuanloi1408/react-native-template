import { BaseDispatchToProps } from "src/core/redux/BaseProps";

export const mapStateToProps = ({ timekeeping, logevent }) => ({});

export const mapDispatchToProps = {
    ...BaseDispatchToProps,
};
